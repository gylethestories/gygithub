/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { MMulyiSelectService } from './m-mulyi-select.service';

describe('Service: MMulyiSelect', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [MMulyiSelectService]
    });
  });

  it('should ...', inject([MMulyiSelectService], (service: MMulyiSelectService) => {
    expect(service).toBeTruthy();
  }));
});
